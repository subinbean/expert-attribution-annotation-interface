const Textbox = (props) => {
    return (
        <header>
            <div className="container">
                <h3> {props.text} </h3>
            </div>
        </header>
    )
}

export default Textbox